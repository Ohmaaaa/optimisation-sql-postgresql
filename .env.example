# Connexion PostgreSQL — copie ce fichier en `.env` et adapte si besoin.
# Le fichier .env réel est ignoré par git (voir .gitignore).
PGHOST=localhost
PGPORT=5432
PGDATABASE=ecommerce
PGUSER=postgres
PGPASSWORD=postgres
